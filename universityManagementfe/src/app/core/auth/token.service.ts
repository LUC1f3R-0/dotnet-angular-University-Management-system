/*
 * Authentication tokens are intentionally NOT stored or read by Angular.
 *
 * access_token  -> HttpOnly cookie
 * refresh_token -> HttpOnly cookie
 *
 * The browser automatically sends them.
 */
export {};
